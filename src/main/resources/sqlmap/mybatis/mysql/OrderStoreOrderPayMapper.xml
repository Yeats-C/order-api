<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiqin.mgs.order.api.dao.OrderStoreOrderPayDao">

    <select id="select" parameterType="com.aiqin.mgs.order.api.domain.OrderStoreOrderPay"
            resultType="com.aiqin.mgs.order.api.domain.OrderStoreOrderPay">
        select id as id,
        pay_id as payId,
        order_id as orderId,
        pay_code as payCode,
        pay_status as payStatus,
        pay_way as payWay,
        order_total as orderTotal,
        activity_money as activityMoney,
        activity_coupon_money as activityCouponMoney,
        cloth_coupon_money as clothOouponMoney,
        actual_money as actualMoney,
        polling_times as pollingTimes,
        pay_start_time as payStartTime,
        pay_end_time as payEndTime,
        create_time as createTime,
        create_by_id as createById,
        create_by_name as createByName,
        update_time as updateTime,
        update_by_id as updateById,
        update_by_name as updateByName
        from order_store_pay
        where 1 = 1
        <if test="id != null and id != ''">
            and id = #{id}
        </if>
        <if test="orderId != null and orderId != ''">
            and order_id = #{orderId}
        </if>
        order by create_time desc
    </select>

    <insert id="insert">
        insert into order_store_pay (pay_id, pay_code, order_id, pay_status, pay_way, activity_money, activity_coupon_money,
                                     cloth_coupon_money, actual_money, polling_times, pay_start_time, pay_end_time,
                                     create_time, create_by_id,
                                     create_by_name, update_time, update_by_id, update_by_name)
        values (#{payId}, #{payCode}, #{orderId}, #{payStatus}, #{payWay}, #{activityMoney}, #{activityCouponMoney},
                #{clothCouponMoney}, #{actualMoney}, #{pollingTimes}, #{payStartTime}, #{payEndTime}, sysdate(),
                #{createById},
                #{createByName}, sysdate(), #{updateById}, #{updateByName})
    </insert>

    <update id="updateByPrimaryKeySelective">
        update order_store_pay
        set
        <if test="payStartTime != null">
            pay_start_time = #{payStartTime},
        </if>
        <if test="payEndTime != null">
            pay_end_time = #{payEndTime},
        </if>
        <if test="payStatus != null and payStatus != '' or payStatus == 0">
            pay_status = #{payStatus},
        </if>
        <if test="payWay != null and payWay != '' or payWay == 0">
            pay_way = #{payWay},
        </if>
        <if test="payCode != null and payCode != ''">
            pay_code = #{payCode},
        </if>
        <if test="pollingTimes != null and pollingTimes != '' or pollingTimes == 0">
            polling_times = #{pollingTimes},
        </if>
        update_by_id = #{updateById},
        update_by_name = #{updateByName},
        update_time = sysdate()
        where id = #{id}
    </update>

</mapper>
